<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Edit Kontak</h1>
    <form action="/contact/<%= contact.nama %>/edit" method="POST">
      <label>Nama:</label><br />
      <input
        type="text"
        name="nama"
        value="<%= contact.nama %>"
        required
      /><br />
      <label>Email:</label><br />
      <input
        type="email"
        name="email"
        value="<%= contact.email %>"
        required
      /><br />
      <label>No HP:</label><br />
      <input
        type="text"
        name="nohp"
        value="<%= contact.nohp %>"
        required
      /><br /><br />
      <button type="submit">Simpan</button>
    </form>
  </body>
</html> -->

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= title %>
  </title>
  <style>
body{
  font-family: 'Poppins', sans-serif;
  
            background: linear-gradient(135deg, #f0f4f8, #ffffff);
}

    /* Mengatur tema warna */
    #body {
      padding-top: 15vh;
      font-family: Arial, sans-serif;
    }

    h1 {
      text-align: center;
      color: rgb(0, 0, 0);
      margin-bottom: 30px;
    }

    .wrap {
      max-width: 400px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(255, 255, 255, 0.1);
    }

    label {
      font-weight: bold;
      color: rgb(0, 0, 0);
    }

    input {
      border-radius: 4px;
      border: 1px solid #ddd;
      padding: 8px;
      width: 100%;
      margin-bottom: 15px;
    }

    button,
    .tombol {
      background: #f4c542;
      color: white;
      border: none;
      border-radius: 4px;
      padding: 10px;
      width: 100%;
      font-size: 16px;
    }

    button,
    .tombol:hover {
      background: #e0b432;
    }
  </style>
</head>

<body>
  <section id="body">

    <h1>Tambah Kontak Baru</h1>

    <!-- Form Kontak Baru -->
    <div class="form-container wrap">
      <form action="/contact/<%= contact.nama %>/edit" method="POST">
        <div class="form-group">
          <label for="nama">Nama:</label>
          <input type="text" class="form-control" value="<%= contact.nama %>" id="nama" name="nama" required />
        </div>

        <div class="form-group0">
          <label for="email">Email:</label>
          <input type="email" class="form-control" value="<%= contact.email %>" id="email" name="email" required />
        </div>

        <div class="form-group">
          <label for="nohp">No HP:</label>
          <input type="text" class="form-control" value="<%= contact.nohp %>" id="nohp" name="nohp" required />
        </div>

        <button type="submit" class="tombol btn-block">Edit</button>
      </form>
    </div>

    <!-- Link ke Bootstrap JS dan dependensi Popper.js -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</section>

</html>